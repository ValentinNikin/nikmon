#pragma once

#include <vector>
#include <string>

namespace nikmon {
namespace stringUtils {
    std::vector<std::string> split(const std::string& str, const char delimiter);
}

namespace utils {

}
}